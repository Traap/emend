# {{{ Include files
includes:
  - app:
    - name: colors
    - name: dotfiles
    - name: git-bash-prompt
    - name: brew

# -------------------------------------------------------------------------- }}}
