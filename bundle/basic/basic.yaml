# {{{ Include files
includes:
  - app:
    - name: colors
    - name: dotfiles
    - name: bash-git-prompt
    - name: brew

# -------------------------------------------------------------------------- }}}
