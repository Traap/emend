# {{{ Installations
installations:
 - os:
   - name: darwin
     command:
      - sudo: false
        program: brew 
        argument: cask install iterm2 

# -------------------------------------------------------------------------- }}}
