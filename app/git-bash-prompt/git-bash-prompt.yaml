# {{{ Repositories
repos:
  - repo:
    url: http://github.com
    paths:
      - source: magicmonty/bash-git-prompt
        target: ~/.bash-git-prompt

# -------------------------------------------------------------------------- }}}
